# ── SportPlanner — Producción ────────────────────────────
# Copiar como .env.prod y completar los valores

# Generar con: php -r "echo 'base64:'.base64_encode(random_bytes(32));"
APP_KEY=base64:REEMPLAZA_CON_TU_CLAVE_AQUI
